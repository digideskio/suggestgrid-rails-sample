<div class="col-md-12">
  <div class="row">
      <div class="col-md-12">
        <h3>Recommended For You</h3>
          <hr/>
          <% @movies.each_slice(4).each do |group| %>
            <div class="row">
                <% group.each do |movie| %>
                <div class="col-sm-3 col-lg-3 col-md-3">
                  <%= render 'movie', {movie: movie, overview: true} %>
                </div>
                <% end %>
            </div>
          <% end %>
      </div>
  </div>
</div>
